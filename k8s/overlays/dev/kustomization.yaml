apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev-infra

resources:
  - ../../base

# Dev-specific patches
patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: tenants-dashboard
      spec:
        replicas: 1
        template:
          spec:
            containers:
            - name: tenants-dashboard
              env:
              - name: NODE_ENV
                value: "development"
              - name: LOG_LEVEL
                value: "debug"

# Image will be built by Tilt
images:
  - name: tenants-dashboard
    newName: tenants-dashboard
    newTag: tilt-dev
