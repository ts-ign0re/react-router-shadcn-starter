apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: production

resources:
  - ../../base

# Production-specific patches
patches:
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: tenants-dashboard
      spec:
        replicas: 3  # HA for production
        template:
          spec:
            containers:
            - name: tenants-dashboard
              env:
              - name: NODE_ENV
                value: "production"
              - name: LOG_LEVEL
                value: "info"
              resources:
                requests:
                  memory: "256Mi"
                  cpu: "200m"
                limits:
                  memory: "1Gi"
                  cpu: "1000m"

# Production image from registry (set by CI/CD)
images:
  - name: tenants-dashboard
    newName: registry.example.com/tenants-dashboard
    newTag: latest  # Will be replaced by CI/CD
